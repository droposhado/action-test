name: 'action-test'
description: 'action to test use of builded docker hub images'
branding:
  icon: 'award'
  color: 'green'
inputs:
  debug:
    required: false
    default: ''
runs:
  using: 'docker'
  image: 'docker://busybox:1.36.1-musl'
  #entrypoint: 'sh -c "echo hello"'
